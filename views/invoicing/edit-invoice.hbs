<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Invoice - Grovix Lab HR Management</title>
    <style>
        /* Reset and basic styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #f5f7fa;
            color: #333;
        }

        /* Header styling */
        .header {
            background-color: #0078e8;
            color: #fff;
            padding: 20px;
            text-align: center;
        }

        .header h1 {
            font-size: 28px;
            margin-bottom: 5px;
        }

        .header p {
            font-size: 16px;
        }

        /* Navbar styling */
        .navbar {
            display: flex;
            justify-content: center;
            background-color: #005bb5;
        }

        .navbar a {
            color: #fff;
            padding: 14px 20px;
            text-decoration: none;
            font-size: 16px;
            text-align: center;
        }

        .navbar a:hover {
            background-color: #0078e8;
        }

        /* Main Content Styling */
        .content {
            padding: 40px;
            max-width: 600px;
            margin: 0 auto;
        }

        .content h2 {
            font-size: 24px;
            color: #005bb5;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Form Styling */
        .invoice-form {
            padding: 20px;
            border-radius: 8px;
        }

        .invoice-form label {
            font-size: 16px;
            color: #333;
            display: block;
            margin-bottom: 5px;
        }

        .invoice-form input[type="text"],
        .invoice-form input[type="date"] {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
            margin-bottom: 15px;
            transition: border-color 0.3s;
        }

        .update-invoice-btn {
            background-color: #0078e8;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            width: 100%;
            text-align: center;
        }

        .update-invoice-btn:hover {
            background-color: #005bb5;
        }

        /* Back Button */
        .back-btn {
            background-color: #ddd;
            color: #333;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            cursor: pointer;
            display: block;
            margin-top: 20px;
            text-align: center;
        }

        .back-btn:hover {
            background-color: #ccc;
        }
    </style>
</head>

<body>

    <!-- Header -->
    <div class="header">
        <h1>Grovix Lab Official Portal</h1>
        <p>Project Management - Update Invoice</p>
    </div>

    <!-- Navbar -->
    <div class="navbar">
        <a href="/">Home</a>
        <a href="/hr">HR</a>
        <a href="/customers">Customers</a>
        <a href="/projects">Projects</a>
        <a href="/invoicing">Invoicing</a>
        <a href="/payments">Payments</a>

    </div>

    <!-- Main Content -->
    <div class="content">
        <h2>Update Invoice</h2>

        <!-- Invoice Form -->
        <form class="invoice-form" action="/invoicing/update-invoice" method="post">
            <label for="project-id">Project ID</label>
            <input type="text" id="project-id" name="projectId" value="{{invoice.projectId}}" required>

            <label for="customer-id">Customer ID</label>
            <input type="text" id="customer-id" name="customerId" value="{{invoice.customerId}}" required>

            <label for="notes">Notes</label>
            <input type="text" id="notes" name="notes" value="{{invoice.notes}}" required>

            <label for="dueDate">Notes</label>
            <input type="date" id="dueDate" name="dueDate" value="{{dueDate}}" required>

            <!-- Update Invoice Button -->
            <button type="submit" class="update-invoice-btn">Update Invoice</button>
        </form>

        <!-- Back Button -->
        <a href="/invoicing" class="back-btn">Back to Invoicing</a>
    </div>

</body>

</html>